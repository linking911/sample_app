<% if @feed_items.any? %>
  <ol class="microposts">
  	<!-- 传入参数给局部视图, partial可以省略， 有collection, 必须用 -->
  	<%= render partial: "shared/feed_item", collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
